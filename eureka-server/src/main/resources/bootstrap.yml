# Need to run the config service first, will read the config automatically when you start eureka server
spring:
  cloud:
    config:
      label: main
      uri: http://localhost:8768/
  application:
    name: eureka-server